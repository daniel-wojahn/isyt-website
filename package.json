{
  "name": "isyt",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "node scripts/check-build.mjs",
    "verify": "npm run build && npm run check"
  },
  "dependencies": {
    "astro": "7.2.0"
  }
}
